# Generated by cargo, and by tauri-build's ACL/schema codegen. Neither belongs in
# the repo, and the root .gitignore's `/target/` is anchored to the repo root, so
# it does not cover this one.
/target/
/gen/schemas/
